\documentclass[lettersize,journal]{IEEEtran}
\usepackage{array}
\usepackage[pdftex]{graphicx}
\usepackage{caption,tabularx,booktabs}
\usepackage{multirow}
\usepackage{algorithm}
\usepackage{algpseudocode}  % replaces \usepackage{algorithmic}
\usepackage{amsmath,amsfonts,amssymb}
\usepackage{adjustbox}
\usepackage{subcaption}
\usepackage[dvipsnames]{xcolor}
\usepackage[T1]{fontenc}
\usepackage{placeins}
\usepackage{tikz}
\usepackage{epstopdf}
\usepackage{float}
\usepackage[hyphens]{url}
\usepackage[pdftex,hidelinks,breaklinks]{hyperref}
\usepackage{mathtools}
\usepackage{multicol}
\usepackage[english]{babel}

\graphicspath{{figures/}}

\begin{document}

\title{Learning Shared Latent Representations for Explainable Multimodal Breast Cancer Detection}

\author{%
Arnav~Jain,
Vaibhav~Baldeva,
Somya~Bansal,
Ruhani~Bhatia,
Mansi~Gambhir,
Simranjit~Kaur,
and~Sachin~Kansal\\[4pt]
\textit{Department of Computer Science and Engineering, Thapar Institute of Engineering and Technology, Patiala, India}\\[2pt]
\texttt{\{ajain5\_be22, vbaldeva\_be22, sbansal1\_be22, rbhatia\_be22, mgambhir\_btech22, simranjit.kaur, sachin.kansal\}@thapar.edu}%
\thanks{Corresponding author email: \texttt{simranjit.kaur@thapar.edu}.}
}

\maketitle


\begin{abstract}
Reliable early screening for breast cancer sits at the intersection of two very different imaging problems, and it is unlikely that any single acquisition modality resolves both at once. Mammography is the widely accepted screening standard for structural detail, yet its sensitivity drops in dense glandular tissue and its use of ionizing radiation remains a well documented concern. Thermography captures vascular and metabolic asymmetry with no radiation exposure at all, but on its own it lacks specificity for downstream clinical action. This work develops a multimodal analysis pipeline that fuses features from both imaging streams at the latent representation level rather than at the raw pixel level. Mammograms flow through a pretrained ResNet50 backbone. Thermograms flow through MobileNetV2. Each embedding is projected into a shared 128 dimensional latent space and aligned using a mean squared error objective, and the aligned representations are then concatenated for downstream inference. Minimum Redundancy Maximum Relevance (mRMR) selection trims the fused vector before the classifier. A dense softmax head then produces the final call between the two clinical classes: benign and malignant. Training uses the CBIS\textendash DDSM mammography dataset and the Visual DMR\textendash IR thermography dataset. Splits are 70 percent train, 15 percent validation and 15 percent test, with class balance preserved across modalities. On the held out test set the proposed pipeline reaches 94.5 percent classification accuracy and a Precision Recall Area Under Curve of 0.991. Under Gaussian noise perturbation the accuracy only slips to 93.1 percent, which is a decent robustness margin. Latent space projections through t\textendash distributed Stochastic Neighbor Embedding (t\textendash SNE) and Principal Component Analysis (PCA) show clean separation between the benign and malignant clusters. SHAP based post hoc analysis then points at the small subset of latent dimensions doing most of the discriminative work. Overall, latent fusion looks like a promising path toward interpretable, research grade breast cancer analysis.
\end{abstract}

\begin{IEEEkeywords}
Breast cancer detection, thermography, mammography, multimodal fusion, latent space alignment, explainable AI, SHAP, CBIS\textendash DDSM, DMR\textendash IR
\end{IEEEkeywords}

\section{Introduction}
Breast cancer sits among the leading causes of mortality among women worldwide. Over 2.3 million new cases are reported each year \cite{sung2021global}. Mammography is still the current gold standard for screening \cite{michell2018role, yala2022deep}, but the diagnostic performance is bounded by several things at once: breast tissue density, exposure to ionizing radiation, and inter observer variability across radiologists. All this has driven a growing interest in add on imaging modalities \cite{ng2022review, arora2008effectiveness}. Infrared thermography stands out. It offers complementary physiological insight and needs no ionizing dose. It is also quick and painless to acquire.

Deep learning has quietly changed what is possible in both mammogram and thermogram analysis. Convolutional networks now pick out lesions, calcifications and vascular asymmetries that a radiologist can miss on visual inspection alone, especially at the earliest stage of disease where the visible signal is small. Reported accuracies on standard mammographic corpora sit above 85 percent in recent years \cite{Shen2019DeepLearningMammo, Ragab2019CBISDDSM, AlMnayyis2025}, and comparable numbers show up on thermal imaging \cite{pereira2020infrared, Roslidar2020, Alzahrani2025}. Beyond raw accuracy, deep models can process thousands of screening cases in minutes. The clinical promise is early detection: catching malignancy patterns that hide inside pixel statistics but are too subtle for the human eye.

Thermography reads temperature asymmetries and vascular heat signatures, and these reflect the underlying metabolic and angiogenic changes that come with malignancy \cite{head1979infrared, ng2009breast}. Recent progress in machine learning and in deep neural networks has revitalized thermographic imaging by allowing automated feature extraction from complex thermal patterns that would be difficult to characterize by hand \cite{pereira2020infrared, el2021machine}. Even so, thermography on its own is often not specific enough to separate benign from malignant heat anomalies, which is one of the main practical reasons it has not replaced structural imaging as a standalone screening modality \cite{vishnu2023review}.

Mammography, in contrast, gives high resolution anatomical and structural detail, and it picks up microcalcifications and soft tissue densities that thermography simply cannot visualize. Mammographic interpretation nevertheless suffers in dense breasts, where the surrounding fibroglandular tissue can obscure lesions and drive false negatives with reduced sensitivity in younger populations \cite{boyd2007mammographic, destounis2021supplemental}. These complementary strengths and weaknesses are exactly what motivates multimodal fusion of thermography and mammography \cite{mostafa2022multi, khan2023hybrid}.

Clinical adoption of any such model still needs one more thing on top of raw accuracy, and that is explainability of the individual decisions produced by the pipeline. Radiologists will not, and arguably should not, trust a black box that emits a probability score with no reasoning attached to it. Post hoc interpretability tools such as SHAP have become important precisely because they identify which features (or which regions of a scan) actually drove a given prediction, and this is a prerequisite both for informed clinical disagreement with the model and for the kind of regulatory audit trails that any real deployment will demand.

The present study develops a multimodal analysis framework that pulls features from thermography and mammography for early breast cancer detection. The thermographic branch uses the Visual DMR\textendash IR dataset \cite{Moreira2014VisualDMR, Silva2018DMRIR}, with quadrant wise segmentation and temperature normalization to capture localized vascular and metabolic patterns, while the mammographic branch uses the CBIS\textendash DDSM dataset \cite{Lee2017CBISDDSM}, from which structural features are extracted through deep convolutional encoders. Both modality streams are then mapped into a shared latent representation, and their joint discriminative behaviour is examined in order to understand how structural and physiological imaging complement each other for breast abnormality classification.

The emphasis of the paper, to be clear, is not on shipping a complete clinical CAD system for direct deployment; it is on understanding the synergistic diagnostic value of combining these two modalities inside the specific context of thermal biology, and the findings suggest that thermography does carry biologically relevant temperature signatures that complement structural mammographic features once they are integrated through latent level multimodal fusion.

\section{Literature Review}

Recent advances in breast cancer imaging cluster around three lines: mammography, thermography and multimodal fusion. The 2024 to 2025 cycle in particular has pushed validation across diverse datasets and complementary imaging modalities.

On the mammography side, Shen et al. \cite{Shen2019DeepLearningMammo} trained a deep network end to end on CBIS\textendash DDSM for the binary benign versus malignant classification task, reporting around 86 percent accuracy in what was one of the earliest reliable numbers on this corpus. Ragab et al. \cite{Ragab2019CBISDDSM} combined a CNN feature extractor with a Support Vector Machine head, again on CBIS\textendash DDSM, and reported around 87 percent for the same binary label set (benign versus malignant). Al\textendash Mnayyis et al. \cite{AlMnayyis2025} developed a CNN classifier for mammographic cancer detection using the KAUH\textendash BCMD dataset, which contains 7{,}205 images from over 5{,}000 patients, and they reported consistent accuracy around 96 percent for the binary benign versus malignant classes across heterogeneous data, with the blunt message that large, clinically diverse validation sets matter more than architectural tweaks. Chen et al. \cite{Chen2025multimodal} extended this line by integrating mammography with ultrasound for lesion classification (binary: benign or malignant), reporting around 91.5 percent test accuracy.

In the thermography domain, Jalloul et al. \cite{Jalloul2024} compared classical and deep learning approaches for infrared breast imaging and reported classification accuracies of 94 to 98 percent using optimized feature extraction together with Support Vector Machines on a binary benign versus malignant label set, and their result confirmed that thermal imaging does encode diagnostically useful physiological information. Alzahrani et al. \cite{Alzahrani2025} built on that direction by designing an end to end CNN enhanced with particle swarm optimization to classify malignant versus benign thermograms, reaching nearly 98 percent accuracy and further demonstrating the viability of thermal signatures once the learning architecture is optimized carefully. Roslidar et al. \cite{Roslidar2020} surveyed deep learning approaches applied to DMR\textendash IR thermograms and reported a wide accuracy range of 85 to 97 percent, with binary settings (benign versus malignant) consistently easier than three class settings (healthy, benign, malignant). Taken together these studies argue that deep learning is a good fit for thermographic breast analysis, even though the datasets remain small in absolute terms.

Parallel to the unimodal side, several studies have chased multimodal fusion so as to combine structural and physiological imaging within a single decision pipeline. Wei et al. \cite{Wei2025} proposed a multimodal framework that aligned ultrasound views with radiology text reports through dual encoders and cross modal feature alignment, reporting around 90 percent binary classification accuracy. Li et al. \cite{Li2025review} provided a systematic review of fusion methodologies and summarized feature level, decision level and hybrid multimodal fusion strategies in a single framework. Hou \cite{Hou2025} carried out a bibliometric analysis of AI assisted multimodal breast imaging and reported a sharp research shift toward integrating MRI, mammography and thermography together rather than in isolation. Mostafa and Hassanien \cite{mostafa2022multi} experimented specifically with feature level fusion of thermographic and mammographic streams and reported around 92 percent accuracy for the binary benign versus malignant setting, and Khan et al. \cite{khan2023hybrid} took a hybrid deep learning route with thermography plus mammography and reported around 94 percent binary accuracy on a mixed cohort.

Collectively, the literature indicates that recent mammography studies have pushed classification performance through larger datasets and cross center validation. Thermography deep models, meanwhile, have demonstrated the diagnostic relevance of physiological heat patterns quite clearly. That said, many existing studies stay unimodal, or skip an interpretable multimodal alignment mechanism, or rely on restricted dataset diversity. The trend toward validated multimodal designs mirrors the motivation of the present work. This work integrates thermal and mammographic modalities through latent level multimodal fusion for interpretable breast cancer analysis.



\section{Methodology}

The proposed multimodal system integrates mammographic and thermographic imaging for robust breast cancer analysis. The design implements feature extraction pipelines that employ deep convolutional backbones, ResNet50 for mammograms and MobileNetV2 for thermograms, followed by standardized fusion, reduction and explainability layers. The implemented code based workflow has three critical stages: (i) modality specific preprocessing and normalization, (ii) deep feature extraction and encoding, and (iii) multimodal feature fusion and classification.
% ----------------------------------------------------------
\subsection{Dataset Description}
\begin{figure}[t]
    \centering

    \subfloat[Benign thermographic sample.]{
        \includegraphics[width=0.22\textwidth]{thermo_benign.png}
    }
    \hfill
    \subfloat[Malignant thermographic sample.]{
        \includegraphics[width=0.22\textwidth]{thermo_malignant.png}
    }

    \vspace{0.2cm}

    \subfloat[Benign mammographic sample.]{
        \includegraphics[width=0.22\textwidth]{noncancer_mammo.png}
    }
    \hfill
    \subfloat[Malignant mammographic sample.]{
        \includegraphics[width=0.22\textwidth]{cancer_mammo.png}
    }

    \caption{Representative thermographic and mammographic samples used in this study. Thermographic images illustrate temperature distribution differences, while mammographic images demonstrate structural variations between benign and malignant cases.}

    \label{fig:dataset_samples}
\end{figure}
\subsubsection{Mammography Dataset: CBIS\textendash DDSM}

The mammographic data are derived from the Curated Breast Imaging Subset of the Digital Database for Screening Mammography (CBIS\textendash DDSM) \cite{Lee2017CBISDDSM}. This dataset is an updated and standardized version of the original DDSM repository, curated by the Cancer Imaging Archive (TCIA). It contains 2{,}620 ROI images extracted from digitized film mammograms encompassing approximately 1{,}500 patients. Each image is annotated with:
\begin{itemize}
    \item Lesion type: \textit{mass, calcification}
    \item Pathology: \textit{benign, malignant}
    \item Breast density (BI\textendash RADS I to IV)
    \item Bounding boxes and pixel level lesion masks
\end{itemize}
All mammograms were converted from 16 bit grayscale to 8 bit, normalized to the range $[0,1]$, and resized to $224\times224$ pixels. The images were then reshaped into 3D tensors of size $224\times224\times3$ by channel replication so that they became compatible with convolutional backbones.

\subsubsection{Thermography Dataset: Visual DMR\textendash IR}

The thermographic data were obtained from the Visual DMR\textendash IR dataset \cite{Moreira2014VisualDMR, Silva2018DMRIR}. It consists of 200 participants imaged under standardized ambient conditions (20 to 22 degrees Celsius, 40 to 60 percent humidity). Each subject was imaged using a FLIR A320 infrared camera capturing frontal and oblique views of both breasts. Each thermogram is a temperature matrix $T(x,y)$ corresponding to surface thermal emission in Kelvin. Each is categorized as \textit{healthy}, \textit{benign}, or \textit{malignant} based on biopsy confirmed outcomes. Representative samples from both imaging modalities are shown in Fig.~\ref{fig:dataset_samples}. The thermographic images illustrate differences in thermal distribution patterns, while the mammographic images demonstrate structural variations associated with benign and malignant breast abnormalities.

\paragraph{Sampling rationale} A note on data volume is in order. Both datasets ship with more raw material than we actually use here. CBIS\textendash DDSM offers thousands of full mammograms and their ROI crops; we sampled a balanced subset of 2{,}620 ROI images so as to match approximately what the DMR\textendash IR side could support, and so as to keep the two class distributions comparable across modalities. On the thermography side, DMR\textendash IR has been recorded from 200 subjects, and we sampled frontal and oblique frames from a subset of these to keep acquisition geometry consistent. The choice to sample, rather than run every available image, was driven by two things. First, we wanted class balance across the two datasets so that the latent alignment step would not learn a spurious modality prior. Second, using the full corpora at their native size would have shifted the emphasis away from the fusion mechanism itself, which is the actual object of study here.

To enable multimodal analysis, mammographic and thermographic samples were aligned at the class level based on diagnostic category labels rather than direct patient wise correspondence, since the CBIS\textendash DDSM and Visual DMR\textendash IR datasets come from independent cohorts. The datasets were partitioned independently into training, validation and testing splits using a 70 / 15 / 15 percent ratio while preserving class balance across modalities. For the binary classification setup, DMR\textendash IR healthy and benign cases were grouped into the benign class, so that both modalities carry the same two label semantics: benign and malignant.



% ----------------------------------------------------------
\subsection{Preprocessing Pipeline}
Fig.~\ref{fig:framework} illustrates the complete workflow of the proposed multimodal thermo mammographic framework, including preprocessing, feature extraction, latent alignment, fusion, classification and explainability stages.
The preprocessing phase ensures modality specific radiometric consistency and prepares both image streams for deep feature embedding. For a given mammogram $I_m(x,y)$ and thermogram $I_t(x,y)$, the overall transformation is formalized as:
\begin{equation}
\mathcal{P} = \{P_M(I_m), P_T(I_t)\} \rightarrow \{I_m', I_t'\}
\label{eq:preproc_set}
\end{equation}
where $P_M$ and $P_T$ denote the preprocessing operators applied to mammography and thermography respectively.

\subsubsection{Mammogram Preprocessing (ResNet50 Pipeline)}

The mammographic preprocessing routine begins by reading raw DICOM or digital mammogram images and converting them into 8 bit grayscale arrays. The following steps are then executed in order:

\begin{enumerate}
    \item \textbf{Histogram Equalization:} The grayscale image is contrast normalized using adaptive histogram equalization to enhance lesion boundaries and glandular tissue visibility \cite{Pizer1987CLAHE}.
    \item \textbf{Standardization:} Pixel intensities are normalized to zero mean and unit variance so that acquisition bias is removed:
    \begin{equation}
    I_m'(x,y) = \frac{I_m(x,y) - \mu_m}{\sigma_m}
    \label{eq:mammo_norm_code}
    \end{equation}
    where $\mu_m$ and $\sigma_m$ denote the mean and standard deviation of pixel intensities respectively.
    \item \textbf{Dynamic Range Scaling:} Normalized values are linearly rescaled to $[0,255]$ so as to preserve the image's dynamic range:
    \begin{equation}
    I_m''(x,y) = 255 \times \frac{I_m'(x,y) - \min(I_m')}{\max(I_m') - \min(I_m')}
    \label{eq:mammo_rescale}
    \end{equation}
    \item \textbf{Resizing and RGB Expansion:} The mammograms are resized to $224\times224$ and replicated across three channels to form $I_m''' \in \mathbb{R}^{224\times224\times3}$. This step ensures compatibility with the CNN architectures used downstream \cite{He2016ResNet}.
\end{enumerate}

\begin{figure*}[t]
    \centering
    \includegraphics[width=\textwidth]{flowchart.png}
    \caption{Overall workflow of the proposed multimodal thermo mammographic breast cancer detection framework, including preprocessing, feature extraction, latent space alignment, multimodal fusion, classification and SHAP based explainability.}
    \label{fig:framework}
\end{figure*}

The processed image is then passed to the ResNet50 backbone for deep feature embedding. This leverages pretrained convolutional kernels that extract hierarchical structural patterns.

\subsubsection{Thermogram Preprocessing (MobileNetV2 Pipeline)}

Thermal imaging needs specialized handling because of varying emissivity and thermal contrast. The preprocessing here ensures consistent temperature scaling and noise suppression prior to deep encoding.

\begin{enumerate}
    \item \textbf{Input Normalization:} Raw thermal maps are converted from absolute temperature matrices to 8 bit grayscale intensity maps. Normalization is performed as:
    \begin{equation}
    T'(x,y) = \frac{T(x,y) - \min(T)}{\max(T) - \min(T)} \times 255
    \label{eq:temp_norm_code}
    \end{equation}
    This transformation ensures uniform pixel intensity scaling across subjects.
    \item \textbf{Noise Suppression:} Gaussian blur with kernel size $(3\times3)$ removes sensor noise and thermal inconsistencies without degrading vascular features.
    \item \textbf{Segmentation:} A binary mask is generated so as to isolate the breast region based on intensity thresholding:
    \begin{equation}
    M_t(x,y) =
    \begin{cases}
    1, & \text{if } T'(x,y) > \theta \\
    0, & \text{otherwise}
    \end{cases}
    \label{eq:thermo_mask}
    \end{equation}
    where $\theta$ denotes a fixed temperature based threshold.
    \item \textbf{Resizing and Channel Duplication:} Each thermogram is resized to $224\times224$ pixels and converted to a three channel format so that it becomes compatible with the deep network downstream \cite{Sandler2018MobileNetV2}.
\end{enumerate}

Both preprocessing pipelines ensure that mammographic and thermographic tensors are normalized, spatially consistent and radiometrically calibrated for deep learning based feature extraction.

To improve generalization and reduce overfitting, data augmentation was applied during training. Mammographic and thermographic images were augmented using random horizontal flipping, rotational transformations within $\pm 10^\circ$, minor zoom variations and small intensity perturbations. These augmentations improve robustness to acquisition variability. They also enhance the ability of the model to learn invariant thermal and structural representations.
% ----------------------------------------------------------
\subsection{Feature Extraction Framework}

Feature extraction employs convolutional backbones pretrained on ImageNet, then fine tuned so as to capture domain specific representations. Mammography and thermography branches operate independently before multimodal fusion.

\paragraph{Why ResNet50 for mammography and MobileNetV2 for thermography.} The two backbones were not chosen at random. Mammograms carry rich high frequency structural information: microcalcification clusters, spiculations, tissue density gradients. Residual connections in ResNet50 let very deep filter stacks propagate gradients cleanly, which is exactly what one wants for these fine grained structural cues. The 2048 dimensional embedding it produces is expressive enough to encode subtle structural anomalies without collapsing them into a shorter code. Thermograms, on the other hand, are lower in spatial detail, noisier at the sensor level, and the DMR\textendash IR corpus is much smaller than a typical natural image dataset. Deep heavy backbones tend to overfit here. MobileNetV2, with its inverted residuals and depthwise separable convolutions, is a much better fit in this regime. Its compact 1280 dimensional embedding is well suited to smaller data volumes and captures the coarser vascular and metabolic asymmetries that the thermal signal actually carries. In short: ResNet50 gives us depth where depth pays off; MobileNetV2 gives us compactness where compactness is a defence against overfitting.

\subsubsection{Mammographic Feature Encoding}

Each preprocessed mammogram $I_m'''$ is fed into the ResNet50 convolutional backbone \cite{He2016ResNet}. The final fully connected layer is removed, and global average pooling is applied so as to yield a compact, high level feature representation:
\begin{equation}
f_M = \phi_M(I_m'''; \theta_M)
\label{eq:resnet}
\end{equation}
where $\phi_M$ represents the feature extraction function and $\theta_M$ are the pretrained parameters. The resulting feature vector $f_M \in \mathbb{R}^{2048}$ captures complex structural and textural information such as tissue density, microcalcifications and spiculations that matter for malignancy prediction.

\subsubsection{Thermographic Feature Encoding}

Thermographic data are processed using MobileNetV2, a lightweight network optimized for computational efficiency and reduced overfitting on comparatively smaller datasets \cite{Sandler2018MobileNetV2}. Each normalized thermogram $I_t'$ is passed through MobileNetV2 as:
\begin{equation}
f_T = \phi_T(I_t'; \theta_T)
\label{eq:mobilenet}
\end{equation}
where $\phi_T$ represents the thermographic encoder and $\theta_T$ are pretrained weights. The extracted features $f_T \in \mathbb{R}^{1280}$ capture vascular, metabolic and symmetry related patterns that correspond to potential malignancy indicators.

% ----------------------------------------------------------
\subsection{Multimodal Feature Fusion and Classification}

Once modality specific feature vectors $f_M$ and $f_T$ are in hand, they are concatenated at the feature level so as to form a unified multimodal representation:
\begin{equation}
\mathbf{F}_{fusion} = [f_M \Vert f_T]
\label{eq:concat}
\end{equation}
This fusion captures complementary information, combining the structural sharpness of mammography with the functional insight of thermography \cite{Khan2023Hybrid}.

Concatenated deep representations tend to introduce feature redundancy and correlated latent activations though, especially in multimodal settings. So dimensionality reduction is performed using the Minimum Redundancy Maximum Relevance (mRMR) criterion \cite{Peng2005mRMR}:
\begin{equation}
\mathcal{S}^* = \arg\max_{\mathcal{S}} \left[ \frac{1}{|\mathcal{S}|}\sum_{x_i \in \mathcal{S}} I(x_i; c) - \frac{1}{|\mathcal{S}|^2}\sum_{x_i, x_j \in \mathcal{S}} I(x_i; x_j) \right]
\label{eq:mrmr_code}
\end{equation}
where $I(\cdot)$ denotes mutual information and $c$ is the class label. The reduced feature subset $\mathcal{S}^*$ is then fed into a fully connected neural classifier:
\begin{equation}
\hat{y} = \mathrm{Softmax}(W_c \mathbf{F}_{fusion} + b_c)
\label{eq:classifier_code}
\end{equation}
The classifier predicts the probability of malignancy using categorical cross entropy loss:
\begin{equation}
\mathcal{L} = -\frac{1}{N}\sum_{i=1}^{N}\sum_{k=1}^{C}y_{ik}\log(\hat{y}_{ik})
\label{eq:loss}
\end{equation}
where $y_{ik}$ and $\hat{y}_{ik}$ represent ground truth and predicted probabilities for class $k$ respectively.

% ----------------------------------------------------------
\vspace{-0.5em}
\subsection{Explainability with SHAP}
\vspace{-0.2em}

Post hoc interpretability is achieved using SHAP (SHapley Additive exPlanations) \cite{Lundberg2017SHAP}. SHAP provides a unified measure of feature importance by decomposing the model output into additive contributions of each input variable:
\begin{equation}
f(\mathbf{x}) = f(\mathbf{x'}) + \sum_{i=1}^{n}\phi_i
\label{eq:shap_code}
\end{equation}
where $\phi_i$ represents the marginal contribution of feature $x_i$ to the prediction. SHAP values were computed for both modalities and mapped so as to visualize how individual features (for example lesion asymmetry, temperature gradient) influenced the malignancy prediction. This makes the decision process transparent, which is important for interpretable medical decision support systems.

\begin{algorithm}[H]
\caption{Multimodal Feature Extraction and Fusion for Breast Cancer Classification}
\begin{algorithmic}[1]
\Require $\mathcal{D}_M = \{I_m^{(i)}, y^{(i)}\}_{i=1}^{N}$, $\mathcal{D}_T = \{I_t^{(i)}, y^{(i)}\}_{i=1}^{N}$
\Ensure $\hat{y}^{(i)} \in \{\text{benign}, \text{malignant}\}$ for each subject $i$

\State Initialize parameters $\theta_M$, $\theta_T$, $W_c$, $b_c$
\State Load mammographic and thermographic datasets; perform independent 70 / 15 / 15 percent train, validation and test splits while preserving class balance

\medskip
\Comment{\textit{--- Preprocessing ---}}
\For{each subject $i = 1$ to $N$}
    \State Histogram equalize $I_m^{(i)}$; normalize via Eq.~\eqref{eq:mammo_norm_code}; rescale via Eq.~\eqref{eq:mammo_rescale}
    \State Resize and replicate channels $\Rightarrow I_m''' \in \mathbb{R}^{224\times224\times3}$
    \State Normalize $T^{(i)}$ via Eq.~\eqref{eq:temp_norm_code}; apply Gaussian blur; threshold mask via Eq.~\eqref{eq:thermo_mask}
    \State Resize thermogram $\Rightarrow I_t' \in \mathbb{R}^{224\times224\times3}$
\EndFor

\medskip
\Comment{\textit{--- Feature Extraction ---}}
\For{each subject $i = 1$ to $N$}
    \State $f_M^{(i)} \gets \phi_M(I_m'''; \theta_M)$ \hfill $\triangleright$ ResNet50, $f_M \in \mathbb{R}^{2048}$
    \State $f_T^{(i)} \gets \phi_T(I_t'; \theta_T)$ \hfill $\triangleright$ MobileNetV2, $f_T \in \mathbb{R}^{1280}$
\EndFor

\medskip
\Comment{\textit{--- Fusion and Dimensionality Reduction ---}}
\For{each subject $i = 1$ to $N$}
    \State $\mathbf{F}_{\text{fusion}}^{(i)} \gets [f_M^{(i)} \Vert f_T^{(i)}]$ \hfill $\triangleright$ Eq.~\eqref{eq:concat}
    \State $\mathcal{S}^* \gets \text{mRMR}(\mathbf{F}_{\text{fusion}})$ \hfill $\triangleright$ Eq.~\eqref{eq:mrmr_code}
\EndFor

\medskip
\Comment{\textit{--- Training ---}}
\For{each mini batch $\mathcal{B} \subseteq \mathcal{S}^*$}
    \State $\hat{y} \gets \mathrm{Softmax}(W_c \mathbf{F}_{\text{fusion}} + b_c)$ \hfill $\triangleright$ Eq.~\eqref{eq:classifier_code}
    \State Compute $\mathcal{L}$ via Eq.~\eqref{eq:loss}
    \State Update $\{\theta_M, \theta_T, W_c\}$ via Adam, $\eta = 10^{-4}$
\EndFor

\medskip
\Comment{\textit{--- Evaluation and Explainability ---}}
\State Evaluate on test set: Accuracy, Precision, Recall, AUC
\State Compute SHAP values $\phi_i$ via Eq.~\eqref{eq:shap_code}
\State Visualize SHAP heatmaps and return $\{\hat{y}^{(i)}, \phi_i\}$

\end{algorithmic}
\end{algorithm}
\vspace{-1em}


\subsection{Experimental Configuration}

All experiments were carried out using Python and TensorFlow on an NVIDIA GPU enabled environment. The multimodal framework was trained using the Adam optimizer with a learning rate of $10^{-4}$ and categorical cross entropy loss. Training ran for 50 epochs with a batch size of 32 under mini batch optimization. Early stopping was applied so as to reduce overfitting. Performance evaluation was carried out using accuracy and PR AUC metrics on held out test data. Gaussian noise perturbation experiments were additionally performed to evaluate robustness under input variability.

\subsection{Implementation Details}

The proposed framework was implemented using TensorFlow and Keras in Python. ResNet50 and MobileNetV2 backbones pretrained on ImageNet were fine tuned for mammographic and thermographic feature extraction respectively. During preprocessing, all input images were resized to $224 \times 224$ pixels and normalized prior to feature encoding.

The ResNet50 encoder generated 2048 dimensional mammographic embeddings, while MobileNetV2 produced 1280 dimensional thermographic embeddings. These representations were projected into a shared 128 dimensional latent space using fully connected embedding layers with ReLU activation. Latent alignment was optimized using mean squared error loss during pretraining.

The fused latent representation was then passed through a lightweight fully connected classifier consisting of dense and dropout layers before softmax prediction. Model training was performed with the Adam optimizer at a learning rate of $10^{-4}$ and batch size of 32. Early stopping and validation monitoring were used so as to reduce overfitting during training.

\section{Results and Analysis}

\begin{table*}[h]
\caption{Comparison of Breast Cancer Detection Performance Across Different Model Variants (Class Labels: Benign versus Malignant).}
\label{tab:comparison}
\renewcommand{\arraystretch}{1.5}

\centering
\begin{tabular}{lcccc}
\hline
\textbf{Model Variant} & \textbf{Accuracy (\%)} & \textbf{PR\textendash AUC} & \textbf{Noisy Accuracy (\%)} & \textbf{Classes} \\
\hline
Thermography Only & 86.2 & 0.912 & 82.7 & Benign / Malignant \\
Mammography Only & 89.4 & 0.941 & 85.9 & Benign / Malignant \\
Fusion (No Alignment) & 91.8 & 0.967 & 89.1 & Benign / Malignant \\
Fusion + Alignment (Proposed) & \textbf{94.5} & \textbf{0.991} & \textbf{93.1} & Benign / Malignant \\
\hline
\end{tabular}
\end{table*}

\begin{table*}[t]
\caption{Comparison with Prior Work on the Same CBIS\textendash DDSM and DMR\textendash IR Benchmarks. Unimodal and Multimodal Studies are Both Reported. Accuracy is Reported as in the Cited Source.}
\label{tab:literature_comparison}
\renewcommand{\arraystretch}{1.3}
\centering
\begin{tabular}{lllcl}
\hline
\textbf{Study} & \textbf{Modality} & \textbf{Dataset} & \textbf{Accuracy (\%)} & \textbf{Class Labels} \\
\hline
Shen et al. \cite{Shen2019DeepLearningMammo} & Mammography (unimodal) & CBIS\textendash DDSM & 86.1 & Benign / Malignant \\
Ragab et al. \cite{Ragab2019CBISDDSM} & Mammography (unimodal) & CBIS\textendash DDSM & 87.2 & Benign / Malignant \\
Al\textendash Mnayyis et al. \cite{AlMnayyis2025} & Mammography (unimodal) & KAUH\textendash BCMD & 96.4 & Benign / Malignant \\
Jalloul et al. \cite{Jalloul2024} & Thermography (unimodal) & DMR\textendash IR & 94.0 to 98.0 & Benign / Malignant \\
Alzahrani et al. \cite{Alzahrani2025} & Thermography (unimodal) & DMR\textendash IR & 98.0 & Benign / Malignant \\
Roslidar et al. \cite{Roslidar2020} & Thermography (unimodal) & DMR\textendash IR & 85.0 to 97.0 & Binary and 3 class (Healthy / Benign / Malignant) \\
Mostafa \& Hassanien \cite{mostafa2022multi} & Multimodal (feature level) & CBIS\textendash DDSM + DMR\textendash IR & 92.3 & Benign / Malignant \\
Khan et al. \cite{khan2023hybrid} & Multimodal (hybrid DL) & CBIS\textendash DDSM + DMR\textendash IR & 93.7 & Benign / Malignant \\
Chen et al. \cite{Chen2025multimodal} & Multimodal (mammo + US) & Private cohort & 91.5 & Benign / Malignant \\
Wei et al. \cite{Wei2025} & Multimodal (US + text) & Private cohort & 90.8 & Benign / Malignant \\
\textbf{Ours (latent fusion + mRMR)} & \textbf{Multimodal} & \textbf{CBIS\textendash DDSM + DMR\textendash IR} & \textbf{94.5} & \textbf{Benign / Malignant} \\
\hline
\end{tabular}
\end{table*}

\begin{figure*}[t]
    \centering
    \includegraphics[width=0.9\textwidth]{latent_feature_importance_ranking.png}
    \caption{Latent feature importance ranking based on mean absolute SHAP values.}
    \label{fig:shap}
\end{figure*}
\subsection{Ablation Study and Comparative Analysis}

To quantify the contribution of individual components of the proposed framework, an ablation study was carried out. It systematically evaluated unimodal baselines, multimodal fusion without latent alignment, and the full proposed model. As shown in Table~\ref{tab:comparison}, multimodal fusion consistently outperforms unimodal approaches. This confirms the complementary nature of thermographic and mammographic information for the binary benign versus malignant task. The mammography only branch reaches 89.4 percent accuracy and PR AUC 0.941. The thermography only branch trails at 86.2 percent accuracy and PR AUC 0.912. Naive concatenation without alignment pushes accuracy to 91.8 percent. Adding the latent alignment step lifts it further to 94.5 percent, with PR AUC 0.991.

Incorporating latent space alignment yields a clear performance gain over unaligned fusion. This confirms its role in reducing modality induced representation discrepancies. The robustness evaluation under noisy conditions indicates only a marginal performance degradation, which highlights the stability and generalization capacity of the proposed approach.

Table~\ref{tab:literature_comparison} places the proposed model against past work on the same CBIS\textendash DDSM and DMR\textendash IR benchmarks. On CBIS\textendash DDSM alone, the strongest unimodal mammography baselines cluster around 86 to 87 percent accuracy \cite{Shen2019DeepLearningMammo, Ragab2019CBISDDSM}. On DMR\textendash IR alone, thermography deep models reach anywhere from 85 to 98 percent depending on architecture and label granularity \cite{Jalloul2024, Alzahrani2025, Roslidar2020}. Existing multimodal designs that combine mammographic and thermographic streams sit at 92 to 94 percent \cite{mostafa2022multi, khan2023hybrid}. The proposed latent fusion pipeline reaches 94.5 percent binary accuracy, which is on par with or above these multimodal baselines and considerably above unimodal mammography. The comparison is indicative and not head to head, since cohort composition and class definitions vary between studies. Still, the direction is consistent: adding thermography to mammography, and aligning the two representations, helps.

\subsection{Multimodal Latent Space Alignment}
To integrate heterogeneous imaging modalities effectively, separate encoders were trained for thermographic and mammographic feature representations. Each encoder compressed modality specific feature vectors of dimension 2048 for mammography and 1280 for thermography into a shared 128 dimensional latent space. During a dedicated pretraining phase, a mean squared error (MSE) based alignment loss was applied so as to minimize the representational discrepancy between corresponding thermographic and mammographic embeddings.

After convergence of the alignment training phase, the latent distributions of both modalities showed improved consistency. This enables meaningful multimodal fusion. The alignment step reduces modality specific bias and ensures that the subsequent fusion classifier operates on semantically compatible representations.

\subsection{Latent Fusion Classification Performance}

% \begin{figure*}[t]
%     \centering
%     \includegraphics[width=0.9\textwidth]{fusion_loss_accuracy.png}
%     \caption{Training and validation loss and accuracy curves of the multimodal fusion model, demonstrating stable convergence and minimal overfitting.}
%     \label{fig:fusion_curves}
% \end{figure*}

\begin{figure*}[t]
    \centering
    \subfloat[Training and validation loss curve.\label{fig:fusion_loss}]{
        \includegraphics[width=0.45\textwidth]{Fusion_model_loss_curve.png}
    }
    \hfill
    \subfloat[Training and validation accuracy curve.\label{fig:fusion_accuracy}]{
        \includegraphics[width=0.45\textwidth]{Fusion_model_accuracy_curve.png}
    }
    \caption{Training dynamics of the multimodal fusion model: (a) loss convergence and (b) accuracy progression across epochs, demonstrating stable optimization and minimal overfitting.}
    \label{fig:fusion_curves}
\end{figure*}


Once latent alignment converged, the outputs of both encoders were concatenated so as to form a unified multimodal embedding of 256 dimensions. A lightweight fully connected classifier was trained on these fused representations for binary breast cancer classification (benign versus malignant).

As shown in Fig.~\ref{fig:fusion_curves}, the fusion model shows smooth convergence across training and validation phases. These complementary properties motivate multimodal fusion of thermography and mammography.

\subsection{Precision Recall Analysis}

\begin{figure}[t]
    \centering
    \includegraphics[width=0.45\textwidth]{precision_recall_curve.png}
    \caption{Precision Recall curve of the fusion model on the test set, illustrating strong discriminative capability across varying decision thresholds.}
    \label{fig:pr_curve}
\end{figure}

Given the class imbalance inherent in medical diagnostic tasks, performance was further evaluated using the Precision Recall Area Under Curve (PR AUC) metric. The high PR AUC value achieved by the fusion model, illustrated in Fig.~\ref{fig:pr_curve}, indicates that the model balances sensitivity and precision effectively. This is critical for a breast cancer screening pipeline.

\subsection{Robustness to Input Perturbations}
To probe robustness we injected zero mean Gaussian noise independently into the thermographic and the mammographic test feature vectors and re evaluated the classifier under this perturbed input regime. The fusion model held its performance. Test accuracy under noise was 93.1 percent against 94.5 percent on the clean test set, a drop of only 1.4 percentage points, and there was no accompanying collapse in the precision recall curve. The fact that each modality stream individually degrades more sharply under the same perturbation but the joint model does not suggests that the two feature branches are absorbing different failure modes and that the alignment step is compensating for them at the latent level rather than at the raw feature level.


\subsection{Explainability via SHAP Analysis}



Fig.~\ref{fig:shap} plots the mean absolute SHAP value across every latent feature dimension. The distribution is sparse. Only a small number of coordinates carry substantial contribution mass while the vast majority sit at or near zero and are effectively silent during inference on the held out test set. This is diagnostic of a classifier that is not treating all 128 latent dimensions equivalently but is instead concentrating its evidence on a compact informative subset something that is desirable both because it makes the downstream SHAP explanations easier for a clinician to interpret and because it hints at redundancy in the raw fused representation that a future dimensionality reduction step could exploit.


\subsection{Latent Space Separability}

\subsubsection{t\textendash SNE Visualization}

\begin{figure}[t]
    \centering
    \includegraphics[width=0.45\textwidth]{latent_space_seperability.png}
    \caption{Two dimensional t\textendash SNE projection of fused latent representations, showing separability between benign and malignant samples.}
    \label{fig:tsne}
\end{figure}

Fig.~\ref{fig:tsne} shows a two dimensional t\textendash SNE projection of the fused latent embeddings on the held out test set. Benign and malignant samples land in visibly separated clusters. Some overlap persists in the central region of the embedding as one would expect for a continuous underlying pathology axis, however the bulk of each class occupies its own distinct region of the projected space and this separation is what we want to see out of a representation that has been aligned across the two modalities at the latent level.

\subsubsection{PCA Based 3D Projection}

\begin{figure}
    \centering
    \includegraphics[width=0.45\textwidth]{3d_latent_space.png}
    \caption{Three dimensional Principal Component Analysis (PCA) projection of multimodal latent embeddings, illustrating class wise separation across principal components.}
    \label{fig:pca3d}
\end{figure}

The three dimensional PCA projection in Fig.~\ref{fig:pca3d} corroborates the same observation along the principal component axes of the fused latent representation. Class separation is present. The boundary between the two clusters however is not a hard axis aligned partition and instead appears as a smooth transition in principal component space suggesting that the classifier has recovered a continuous disease severity manifold rather than an axis aligned hyperplane between benign and malignant.

\subsection{Summary of Findings}
Bringing the experimental results together the multimodal latent fusion pipeline meets the objectives that were laid out at the start of this study. Thermographic and mammographic information are combined at the latent representation level rather than at raw pixel level and the joint classifier reaches 94.5 percent test accuracy with a PR AUC of 0.991 on the binary benign versus malignant task. Robustness under Gaussian input noise stands at 93.1 percent, only a 1.4 percentage point drop from the clean baseline. Latent space projections through t\textendash SNE and PCA confirm class wise separability, while the SHAP analysis provides per feature attribution over the fused latent representation.

Read together these observations argue that latent level multimodal fusion is a workable and reasonably interpretable design choice for breast cancer analysis within a research oriented diagnostic setting.


\section{Conclusion and Future Work}
This paper presented a multimodal breast cancer detection pipeline that routes thermographic and mammographic imagery through independent modality specific encoders. Each encoder projects its features into a common 128 dimensional latent space and an alignment objective drives the two projections toward each other in that space so that the corresponding cross modal representations become geometrically comparable at the latent level. The aligned latent embeddings are then concatenated and passed to a binary classifier that discriminates benign from malignant instances.

Empirically the pipeline holds up under evaluation. The joint held out test set drawn from CBIS\textendash DDSM and DMR\textendash IR yields 94.5 percent classification accuracy and a PR AUC of 0.991, and injecting zero mean Gaussian noise into both modality feature streams only reduces accuracy to 93.1 percent. Latent space visualisations under t\textendash SNE and PCA show benign and malignant samples occupying visibly separate regions of the projected space. Taken together these signals suggest that the learned latent codes are keying on disease relevant information rather than on modality specific noise or acquisition artefacts.

Beyond the raw performance numbers the paper leaned on SHAP based post hoc interpretability so that individual predictions could be inspected at the level of latent feature contributions. This is not cosmetic. A radiologist reviewing an automated flag will want to see which fused features drove the model toward its call, not only the probability score attached to that call, and the SHAP scaffolding in this pipeline is what makes that kind of per prediction inspection possible in practice.

The overall message of this study is that latent level multimodal fusion is a workable strategy for automated breast cancer analysis. It offers robustness under input noise, a measurable performance gap over the unimodal baseline in the reported ablation, and a route to per prediction interpretability through SHAP. Three concrete directions look promising for follow on work. First, validation of the pipeline on larger and clinically more diverse cohorts ideally with multi site acquisition variability and biopsy confirmed labels. Second, replacement of the current concatenation based fusion with explicit cross modal attention layers between the two backbones so that mammographic and thermographic evidence can weight each other adaptively rather than uniformly. Third, extension of the pipeline to longitudinal and near real time diagnostic scenarios in which the classifier is expected to update its state as new scans arrive for the same patient over time.


\section*{Author Contributions}
The contributions of each author to this work are summarised below using standard CRediT taxonomy roles.
\begin{itemize}
    \item \textbf{Arnav Jain}: Methodology, Software, Investigation, Visualization, Writing \textendash{} original draft.
    \item \textbf{Vaibhav Baldeva}: Methodology, Software, Investigation, Writing \textendash{} original draft.
    \item \textbf{Somya Bansal}: Data curation, Writing \textendash{} original draft (supporting).
    \item \textbf{Ruhani Bhatia}: Validation, Writing \textendash{} original draft (supporting).
    \item \textbf{Mansi Gambhir}: Data curation, Writing \textendash{} original draft (supporting).
    \item \textbf{Simranjit Kaur}: Supervision, Project administration, Writing \textendash{} review and editing.
    \item \textbf{Sachin Kansal}: Supervision, Writing \textendash{} review and editing.
\end{itemize}
All authors read and approved the final manuscript.

\bibliography{refs}
\bibliographystyle{IEEEtran}

\end{document}
